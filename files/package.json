{
  "name": "drift-catalog",
  "version": "1.0.0",
  "description": "DRIFT — a modular vanilla-JS e-commerce product catalog capstone (client-side routing, cart, localStorage persistence).",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.js",
    "build": "node scripts/build.js",
    "preview": "node scripts/dev-server.js dist"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  },
  "license": "MIT"
}
